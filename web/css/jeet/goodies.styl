// Reusable Custom Elements
.chromeframe
    background #fff
    background linear-gradient(top, #fff, #eee)
    border-bottom 1px solid #ddd
    box-shadow inset 0 -1px 0 #fff
    text-shadow 0 1px 0 #fff
    p
        center()
        padding 30px 0
        text-align center

// Buttons
btn(bg_color = #eee, radius = 10px, style = flat)
    background bg_color
    border-radius(radius)
    cursor pointer
    text-align center
    text-decoration none
    if dark(bg_color)
        box-shadow inset 0 1px 0 rgba(255,255,255,.2)
        border 1px solid saturate(darken(bg_color, 15%), 15%)
        color saturate(lighten(bg_color, 85%), 5%)
        text-shadow 0 1px 0 saturate(darken(bg_color, 20%), 30%)
    if light(bg_color)
        box-shadow inset 0 1px 0 rgba(255,255,255,.5)
        border 1px solid saturate(darken(bg_color, 30%), 20%)
        color saturate(darken(bg_color, 60%), 10%)
        text-shadow 0 1px 0 saturate(lighten(bg_color, 55%), 90%)
    if style is flat
        background linear-gradient(top, lighten(bg_color, 10%) 10%, darken(bg_color, 10%) 90%)
    if style is glossy
        background linear-gradient(top, lighten(bg_color, 8%) 50%, darken(bg_color, 8%) 50%)
    &:hover
        opacity(.9)

// Shadow
shadow(w = 90%, args...)
    position relative
    &:before, &:after
        content ''
        position absolute
        z-index -1
    &:before
        display block !important
        bottom 0
        left 0
        right 0
        margin auto
        border-radius 999px
        width w
        height 0
        box-shadow(args)

// Vertical (and Horizontal) Centering for Chrome, Opera, Maxthon as of 1/13
vc()
    display -webkit-flex
    display flex
    -webkit-justify-content center
    justify-content center
    -webkit-align-items center
    align-items center