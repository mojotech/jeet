// This lets you do things like width(fifth) instead of having to do stupid mathz.
// Presentational classes suck, but it's pretty nice when you're making forms and
// want inputs of various sizes and such without having to size 50 nth-childs.

width(numerator = 1, denominator = 1, span = f, center = f, last = l)
    x = (numerator / denominator) * columns
    cf()
    display inline
    float left
    width total_width * ((((gutter_width + column_width ) * x) - gutter_width) / (gridsystem_width - gutter_width))
    margin-right (total_width * ( (gutter_width * 0.5) / (gridsystem_width - gutter_width))) * 2
    *width total_width * ((((gutter_width + column_width ) * x) - gutter_width) / gridsystem_width) - (correction * 2)
    *margin-right (total_width * ( (gutter_width * 0.5) / gridsystem_width)-correction) * 2
    &:last-child
        last()
    if (span = span)
        width total_width * ((((gutter_width + column_width ) * x)) / gridsystem_width)
        *width total_width * ((((gutter_width + column_width ) * x)) / gridsystem_width)-correction
        *behavior url(js/vendor/boxsizing.htc)
        margin-right 0 
    if (center == c)
        float none !important
        display block !important
        margin-right auto !important
        margin-left auto !important
        *margin-right auto !important
        *margin-left auto !important
    if (last == f)
        &:last-child
            margin-right (total_width * ( (gutter_width * 0.5) / (gridsystem_width - gutter_width))) * 2
            *margin-right (total_width * ( (gutter_width * 0.5) / gridsystem_width)-correction) * 2
.full
    width()
.half
    width(1, 2)
.third
    width(1, 3)
.fourth
    width(1, 4)
.fifth
    width(1, 5)
.sixth
    width(1, 6)
.seventh
    width(1, 7)
.eigth
    width(1, 8)
.ninth
    width(1, 9)
.tenth
    width(1, 10)
.eleventh
    width(1, 11)
.twelfth
    width(1, 12)